# PHB_gtdb-ds 项目最新审计报告

> 审计日期：2026-08-24
> 本地项目：`<LOCAL_WORKSPACE>`
> T141 工作区：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds`
> 当前运行：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/runs/20260821_schemeA_03`
> 服务器：`<SERVER_USER>@<SERVER_HOST>`
> 性质：基于本地文件、T141 manifest、结果哈希与修复台账的只读审计快照

## 1. 执行摘要

当前可追溯基线为 `20260821_schemeA_03`。T141 的 `results/run_manifest.json` 登记了 11 个步骤，全部退出码为 0，输入/输出均有 SHA-256；该 manifest 当前实际 SHA-256 为
`e991c3bd10a48f8faf9c450f0c17a5a3fb1f0315c256018f0772c5f64f71b2a3`。项目可以作为“GTDB R232 上候选 PHB/PHA 相关同源蛋白的筛选与分布分析”使用，但不能将 HMM、domain、邻域或系统发育证据写成已经证明的 PHB 降解表型。

审计确认：

- GTDB R232 代表基因组：**199,923**。
- curated seed：**78** 条；reviewed **55**、unreviewed **23**；train **64**、validation **14**。
- 初始扫描使用 **9 个 HMM 家族**；核心分布图采用 4 个去重核心家族：`ePhaZ`、`iPhaZ`、`OH`、`ArchPhaZ_hydrolase`。
- tier1 蛋白记录：ePhaZ **38,692**、iPhaZ **32,926**、OH **1,429**、ArchPhaZ_hydrolase **1,292**；四家族去重后覆盖 **44,814/199,923 = 22.416%**。
- `ArchPhaZ_patatin` 为广谱 patatin fold，单独作古菌候选集合，不计入核心四家族去重总量；其 tier1 文件有 **112,926** 条，不能直接等同于 PHB depolymerase。
- SignalP：ePhaZ 信号肽相关类型 **21,856/38,692 = 56.5%**，OTHER **16,836 = 43.5%**。
- cluster 使用 6 个 marker：`PhaC`、`PhaE`、`PhaJ`、`BdhA`、`phasin`、`PHA_gran_rgn`。
- `batch_001` 两个 FAA 异常 accession 已修复；dated 结果状态为 `accepted_for_dated_result`，未覆盖历史 canonical `results`。
- 本地 `HEAD` 与 GitHub `main` 均为 `9a7d02d`，但本地工作树含未提交修改和未跟踪审计/结果文件；T141 项目根目录没有 `.git`，主 manifest 的 `git_commit` 为 `null`。因此 GitHub、当前本地工作树、T141 dated deploy/run 不是同一可复现快照。

不能宣称：全量 ePhaZ/iPhaZ 树已完成、OH 旧树代表当前输入、古菌 patatin 已验证为 PhaZh1、任一候选已具备实验 PHB 降解活性、基因邻域已证明完整代谢通路。

## 2. 审计范围与证据等级

检查范围包括本地 `pipeline/`、`data/`、`results/`、`docs/`、测试与图件，以及 T141 的主运行目录、`batch_001` repair manifest、forensic 比较 JSON 和 dated 表。

| 等级 | 判定标准 | 当前例子 |
|---|---|---|
| A | manifest、退出码、输入/输出哈希和逐记录对账齐全 | 主运行、batch_001 修复 |
| B | 结果和脚本可复核，但登记不完整 | 部分历史结果/树 |
| C | 探索性或历史汇总 | 旧版邻域汇总、未登记 CD-HIT 树 |
| D | 计划未执行或 review-blocked | 全量树、HGT、实验验证 |

## 3. 架构、路径和数据边界

```
pipeline/config/params.yaml       参数契约
pipeline/seeds/                   seed FASTA、manifest、证据、controls
pipeline/scripts/                 02-11 主流程与审计脚本
data/                             大型输入/派生数据，主要在 T141
results/tables/                   同步结果表
results/trees_tier1/              树文件与 tree_manifest.tsv
results/figures/scheme_a/        Figure 0-5、source_data、QA
docs/                             状态、复现、结果和审计文档
```

- GTDB 原始数据位于 T141 `~/GTDB/...`，项目只读使用。
- FAA/GFF、HMMER 中间文件和 per-genome 工作目录主要在 T141；本地是部分同步快照。
- `results/` 是派生结果，不等于原始数据；必须和 manifest、输入哈希一起解释。
- 历史目录应保留用于对账，不能仅按目录名自动替换当前 dated 结果。

版本边界：dated forensic repair 使用
`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/deploy/20260821_schemeA/scripts/11_clusters.py`
（SHA-256 `d1d907f34a5c1fbe17aee538a3ca087de8e809f5c03286b6384a22347d994d2c`）；服务器根目录
`scripts/11_clusters.py` 是另一版本。后续运行必须从 dated `deploy/<run_id>/scripts/` 或明确绑定的
Git commit 启动，禁止直接运行服务器根目录旧脚本。

## 4. 工作流阶段状态

| 阶段 | 状态 | 审计证据/限制 |
|---|---|---|
| seed 收集、证据和划分 | 已完成 | `seeds_manifest.tsv`、`seeds_stats.json` |
| HMM v2 构建/整理 | 已完成（结果使用） | `data/hmms/v2/` |
| 正/负对照校准 | 已完成，小样本 | `calibration_summary.tsv`；OH 需 coverage 仲裁 |
| Pyrodigal meta 预测 | 已完成并作为输入 | 199,923 目标基因组；异常 FAA 单独修复 |
| HMMER 扫描、hits 处理、序列验证 | 已完成 | `hits_all.tsv`、`hits_filtered.tsv`、tier 输出 |
| tier1/tier2 重评分和分布 | 已完成 | `08c_tier_rescore`、分布表 |
| ePhaZ SignalP | 已完成 | `results/signalp/ePhaZ/` |
| cluster/邻域 | 已完成，dated 修复版本可追溯 | 80 批次、6 marker |
| ArchPhaZ_hydrolase 树 | 已生成、输入哈希一致 | 1,292 leaves IQ-TREE2 |
| OH 树 | review-blocked / stale input | 旧输入 1,465；当前 tier1 1,429 |
| ePhaZ/iPhaZ 树 | 仅抽样/去冗余 | 未完成全量树 |
| HGT、实验表型 | 暂未开始 | 不得写成已完成 |
| GitHub Release / Zenodo | 未完成 | 无最终 DOI |

## 5. 主运行与复现信息

主运行：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/runs/20260821_schemeA_03`。

manifest 步骤依次覆盖：预检、domtblout coverage 聚合、OH `min-cov=0.6` 仲裁、序列提取/验证、tier 重评分、分布、80 批次 cluster、最终冻结。关键参数：

| 参数 | 值 |
|---|---|
| GTDB | `R232` |
| Pyrodigal | `meta` |
| 线程上限 | 70（T141 保留约 10 核） |
| HMMER e-value | `1e-5` |
| 默认 min-cov | `0.0` |
| OH 仲裁 | `min-cov=0.6` |
| tier1/tier2 | `E<1e-20` / `E<1e-10` |
| 邻域窗口 | ±10 kb |
| cluster | 80 批次、最大基因组参数 2,000 |

关键哈希：

| 对象 | SHA-256 |
|---|---|
| 本地 `params.yaml` | `de3593a47b09953338f98f80c8d4e5b7e32bd5a5bbdcd9661e1b0c9599c48e44` |
| `11_clusters.py` | `d1d907f34a5c1fbe17aee538a3ca087de8e809f5c03286b6384a22347d994d2c` |
| `11_merge_cluster_outputs.py` | `2b9456d3ddb206defb36c0553685133a099bf04a405392b007f3d2416df7a409` |
| 本地 `run_manifest.py` | `58732acfa4b738af0e4550926948926a6bd998da14acf31b044a67bba27f3aa4` |
| 主运行 hits_filtered | `6da49e67a681ec680af3c911e366c64f31a966f948de1665da799100dc9d321b` |
| 主运行 hits_all | `5dfee3f6352fa88f0fa85bb32ca4cc5c4017a0f00caa77137823a80afd4cc317` |

运行环境证据（T141 `phb_gtdb` 环境）：Python **3.12.13**、Pyrodigal **3.7.1**、HMMER **3.4**；项目锁定环境入口为 `${PHB_REMOTE_ROOT}/miniconda3/envs/phb_gtdb/bin/python`。重新运行必须重新生成 manifest，不能只比较文件名或修改时间。

## 6. Seed 与 HMM 设计审计

9 个 HMM 按角色分为：

- 核心解聚酶：`ePhaZ`、`iPhaZ`、`OH`、`ArchPhaZ_patatin`、`ArchPhaZ_hydrolase`；
- 背景代谢：`BdhA`；
- 邻域辅助：`PhaJ`、`phasin`、`PhaC`。

`ArchPhaZ_patatin` 是广谱 patatin fold；其特异性必须依赖古菌分类、基因邻域和文献证据联合判断，不能只凭一个 HMM 命中写成 PhaZh1。

## 7. 最新结果与统计单位

### 7.1 家族结果

| 家族 | tier1 蛋白记录 | 口径 |
|---|---:|---|
| ePhaZ | 38,692 | 候选 ePhaZ 同源序列 |
| iPhaZ | 32,926 | 候选 iPhaZ 同源序列 |
| OH | 1,429 | OH coverage 仲裁后的候选序列 |
| ArchPhaZ_hydrolase | 1,292 | 古菌候选 hydrolase 同源序列 |
| ArchPhaZ_patatin | 112,926 | 广谱 patatin 候选，独立报告 |

核心四家族去重结果是 genome 数，不是蛋白记录数：**44,814 个基因组，占 22.416%**。家族可以共存，不能把四个记录数相加当作独立基因组。

`ArchPhaZ_patatin` 的 **112,926** 是广谱 patatin 候选蛋白记录层的数量；古菌 patatin 讨论子集为
**1,372 个 locus / 620 个 genome**。两者不是同一统计层级，均不计入核心四家族去重总量。

本地同步表的记录行数：

- `tier1_genome_family.tsv`：133,829 行；
- `tier1_phylum_distribution.tsv`：313 行；
- `cluster_context.tsv`：1,372 行；
- `cluster_summary.tsv`：4 行；
- `calibration_summary.tsv`：21 行；
- `signalp_ePhaZ_analysis.tsv`：15 行。

### 7.2 分布与 Figure 5 统计口径

Figure 3 的 source data `results/figures/scheme_a/source_data/figure3_phylum_totals.tsv` 是当前图源
权威：其中 Pseudomonadota 为 **26,850** 个 genome。报告/历史表中出现的 **26,855** 是旧下游输出，
已被本审计版本废止；后续表格和图件必须引用 26,850，并保留旧值仅作差异追踪。

Figure 5 的 source data `figure5_neighborhood_rate.tsv` 使用**全命中邻域层**的 `candidate_loci`
作为 denominator；例如 patatin 行的 denominator 为 315,065 个 candidate loci。它既不是核心四家族
tier1 分母，也不是古菌 patatin 子集的 1,372 loci / 620 genomes 分母。因此 Figure 5 的 support rate
不能直接解释为 tier1 或 patatin 子集比例，更不能作为完整代谢通路或 PHB 降解表型证据。

### 7.3 SignalP

| 类型 | 数量 | 比例 |
|---|---:|---:|
| Sec/SPI | 16,545 | 42.8% |
| Lipo/SPII | 5,170 | 13.4% |
| Tat | 122 | 0.3% |
| TatLipo | 19 | 0.05% |
| OTHER | 16,836 | 43.5% |
| 有信号肽合计 | 21,856 | 56.5% |

N 端核查中 Met 开头约 2.8%，且信号肽组与 OTHER 长度接近，因此 N 端截断不是 OTHER 的主要解释。SignalP 仍是定位预测，不是酶活性证据。

## 8. batch_001 FAA 修复审计

### 8.1 修复对象

| accession | 新 FAA 记录 | 新 FAA SHA-256 | 备注 |
|---|---:|---|---|
| `GCA_003604995.1` | 2,307 | `bd70faa47b97795ced884dd0839438f8dba0620c4799b644f48534efe83c6071` | 已重建 |
| `GCA_016125335.1` | 3,731 | `b0154f4528c1b543469393c7a17a17c53b7bf9142313c978dadd677e443d8149` | 原 FAA 为空；已备份 |

空 FAA 备份路径：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/runs/20260821_schemeA_03/data/cluster_batches/batch_001/data/work/GCA_016125335.1.faa.pre_faa_repair_20260824`。备份 SHA-256：`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`。

### 8.2 Forensic 运行

目录：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/runs/20260821_schemeA_03/forensic_full_batch001_repair_20260824_v3`。

- frozen FAA：2,173；frozen GFF：2,173；
- completed genomes：2,173；failed：0；
- audit rows：12,871；所有 locus 状态 `analyzed`；
- 输入 hits SHA-256：`ba0037130d8592f043bb04b52d6710fb5a3082f65fd923094db3e5403ccd8e57`；
- marker HMM：`PhaC/PhaE/PhaJ/BdhA/phasin/PHA_gran_rgn`。

dated 结果：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/runs/20260821_schemeA_03/data/cluster_batches/batch_001/results.faa_repair_20260824`。修复台账：`${PHB_REMOTE_ROOT}/PHB_gtdb-ds/runs/20260821_schemeA_03/data/cluster_batches/batch_001/repair_manifest_20260824.json`，状态 `accepted_for_dated_result`。

### 8.3 对账结论

- 与 canonical `results`：context 行多重集一致；genome audit 字节一致；locus audit 行多重集一致；summary 字节一致。
- 与 `results.pre_faa_remediation_20260823`：同样逐记录等价。
- 与异常 `results.faa_retry_20260823`：恢复 **38 条 context 记录**（2,637 → 2,675）。
- 影响范围仅为两个修复 accession，未发现其他 accession 差异。

结论：dated 修复结果可以接受；历史目录和比较 JSON 必须保留，不能静默覆盖。

dated 结果关键哈希（来自 `repair_manifest_20260824.json`）：

| 文件 | SHA-256 |
|---|---|
| `full_batch_manifest.json` | `4ec37b9d00883ee4eb8c34ce74afbf11ad4c4c9969396fa6dc9b8d1c53d69a87` |
| `tables/cluster_context.tsv` | `de7a3a12a6e499faa0fb7e0fa7aea6ea86163e5ac4bfca545570201ffe1f0b28` |
| `tables/cluster_genome_audit.tsv` | `119f304685324181b488b1c3a30404cdd446ae197d5ca1041c19836215a34cc2` |
| `tables/cluster_locus_audit.tsv` | `b18fddfc0275df0cf42e86cd7560348a1fd45d300e11919553e8a5c612b1545d` |
| `tables/cluster_summary.tsv` | `3110b3023c7f2b4f9c0de2230794265000eb232a0fb75b53cbefb705e03a8e56` |

## 9. 邻域、生物学和古菌解释边界

6 个 marker 只支持“命中位点附近存在相关代谢背景”，不能证明 operon、完整通路或体内/体外 PHB 降解活性。古菌 patatin 候选应统一写为“候选同源序列/功能潜力”；不能写为已验证古菌 PHB 降解基因。

## 10. 树与图件审计

`results/trees_tier1/tree_manifest.tsv`：

- ArchPhaZ_hydrolase IQ-TREE2：1,292 leaves，输入哈希一致，`ok`；
- ArchPhaZ_hydrolase FastTree：1,000 leaves，抽样；
- ePhaZ：FastTree 1,000、IQ-TREE2 1,942，抽样；
- iPhaZ：FastTree 1,000、IQ-TREE2 2,000，抽样；
- OH：登记输入 1,465、当前 tier1 1,429，`stale_input`；
- ePhaZ CD-HIT：10,648 leaves，`input_not_registered`。

清单总体状态为 `ok=6`、`stale_input=2`、`input_not_registered=1`。OH 的旧输入树不能代表当前
1,429 条输入；ePhaZ CD-HIT 树因服务器端去冗余 FASTA 未登记来源与 SHA-256 而不能作为正式树证据。

所以 OH 树必须用当前 1,429 条输入重建后才可用于 scheme-A；ePhaZ/iPhaZ 全量树及 HGT 未完成。

本地 `results/figures/scheme_a/qa_report.json` 为 `pass`，Figure 0-5 均有 SVG/PDF/TIFF/PNG、source data 和像素/SVG 文本 QA。QA 通过不等于生物学结论已验证；核心分布图的 4 家族口径也不等于只扫描了 4 个 HMM。

## 11. 风险与后续建议

高优先级：

1. 用当前 OH tier1 FASTA 重建 OH 树并更新 tree manifest、输入哈希、工具和参数。
2. 统一 README、STATUS、结果报告、handoff 与本报告的权威目录和状态措辞。
3. 为 GTDB 元数据、taxonomy、骨架树、HMM 和 T141 原始输入建立 checksums 清单。
4. 将 dated batch_001 manifest、repair manifest、comparison JSON 和四张表同步建立索引。

中优先级：

1. 分栏报告 ArchPhaZ_patatin 的 fold、古菌分类、邻域和文献证据。
2. 所有表明确“记录数、唯一 locus、唯一 genome”的统计单位。
3. 让 `11_clusters.py` 自动记录 marker panel、输入路径和输入哈希。
4. 绑定 SignalP、分布统计和图源数据的 hash。

发布前：

1. 完成 OH 树人工复核、代表候选序列/结构域边界检查。
2. 树输入、模型、抽样和支持度审查完成后再讨论 HGT。
3. 生成环境锁定文件、数据字典和 GitHub Release/Zenodo 发布包。
4. 追加实验或独立功能验证，才能提升到表型结论。

## 12. 审计接受判定

| 对象 | 判定 |
|---|---|
| 主运行 `20260821_schemeA_03` | 可作为当前分析基线 |
| 核心四家族分布 | 可用于候选同源序列分布描述 |
| ePhaZ SignalP | 可用于信号肽预测描述 |
| batch_001 dated FAA 修复 | **接受为 dated 结果** |
| canonical batch_001/results | 与 dated 结果逐记录等价，保留兼容 |
| OH 旧树 | 不接受为当前 scheme-A 树证据 |
| ePhaZ CD-HIT 树 | 不接受为正式树证据 |
| 古菌 patatin 候选 | 仅候选功能潜力 |
| PHB 降解表型 | 未由本项目计算结果直接证明 |

## 13. 关键路径与检查清单

```
本地配置：pipeline/config/params.yaml
本地 seed：pipeline/seeds/seeds_manifest.tsv
本地 HMM：data/hmms/v2/
本地 tier1：data/screen/tiers/*_tier1.faa
本地结果表：results/tables/
本地树登记：results/trees_tier1/tree_manifest.tsv
本地图件：results/figures/scheme_a/
远程主 manifest：runs/20260821_schemeA_03/results/run_manifest.json
远程修复台账：runs/20260821_schemeA_03/data/cluster_batches/batch_001/repair_manifest_20260824.json
远程 dated 结果：runs/20260821_schemeA_03/data/cluster_batches/batch_001/results.faa_repair_20260824/
远程 forensic：runs/20260821_schemeA_03/forensic_full_batch001_repair_20260824_v3/
```

- [x] 当前运行目录和服务器身份
- [x] 主 manifest、退出码和输入/输出哈希
- [x] seed 数量及划分
- [x] 9-family 扫描与 4-family 展示口径
- [x] tier1、去重 genome、SignalP、cluster marker
- [x] FAA 修复和 forensic 2173 genome / 12871 locus / 0 failure
- [x] dated 与历史结果对账
- [x] stale OH tree、unregistered CD-HIT tree
- [x] 图件 QA pass
- [ ] OH 树按当前输入重建
- [ ] 完整 checksum 发布
- [ ] GitHub Release / Zenodo DOI
- [ ] 实验或独立功能验证

---

## 0.1 本轮修订的口径与权威关系

审计快照中的 `Pseudomonadota` 应以图源数据
`results/figures/scheme_a/source_data/figure3_phylum_totals.tsv` 为准，即
**26,850 个基因组**；报告中出现的 26,855 是旧表述，已废止。Figure 5 的
`candidate_loci` 是 all-hit neighborhood 分母，不能解释为 tier1 分母，也不能
解释为古菌 `ArchPhaZ_patatin` 子集。后者仍须分开报告为 1,372 位点 / 620
基因组；112,926 是广谱 patatin-fold 候选记录，不是已证实的 PHB depolymerase。

本地工作树当前仍是未提交的审计修复工作区；GitHub `main` 停留在
`9a7d02d`，不能当作 2026-08-24 基线。T141 主 manifest 当前 SHA-256 为
`e991c3bd10a48f8faf9c450f0c17a5a3fb1f0315c256018f0772c5f64f71b2a3`，与旧文档
记录的 `c4e8...` 不同。服务器根目录旧 `scripts/` 与 dated deploy 中的脚本
也不是同一版本；后续只能从 dated deploy 或明确绑定的 Git 源码包启动。

本地 `run_pipeline.sh` 已加入严格 provenance 入口，会绑定源码文件、环境、
GTDB manifest、06 屏蔽步骤使用的 v2 HMM、08c 重评分使用的 legacy core HMM
以及每步命令；两套不同 HMM 会同时登记而不会被静默替换。现有 T141 历史 manifest 尚未具备
完整 source-bundle/环境/命令绑定，因此不能追溯性地宣称满足新契约；必须以新
dated run 重新生成。

本报告是 2026-08-24 审计快照。后续重跑、修复、树重建或合并必须生成新的 dated 目录和 manifest，并追加新版本，不覆盖本报告中的历史证据。
